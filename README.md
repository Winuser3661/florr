human authors note: the following is ai generated. It should be quite accurate but there may be mistakes/hallucinations/slop etc., refer to the .md files in [`clientnew/`](clientnew/) for a more accurate overview.
# florr

> Disclaimer: this README, and the analysis it links to, were generated by AI
> from a static read of the game's WebAssembly binary. Treat everything here as
> a best-effort reconstruction, not official documentation. Numbers and names
> are pulled straight from the binary's data section and may be incomplete or
> slightly off. Nothing here is affiliated with or endorsed by the developer of
> florr.io.

This repo is a static reverse-engineering study of the modern florr.io client.
The client ships as a single 9 MB WebAssembly module (Rust compiled to wasm via
wasm-bindgen). The symbol names are stripped and the imports/exports are
minified, so there is no clean decompile, but the 4 MB data section still leaks
the entire content model, the tech stack, and every embedded asset. All of that
is extracted, decoded, and checked in under [`clientnew/`](clientnew/).

## What is here

Everything lives in [`clientnew/`](clientnew/):

- [`clientnew/README.md`](clientnew/README.md) - the folder index (what each file is).
- [`clientnew/analysis.md`](clientnew/analysis.md) - the full static-analysis write-up.
- [`clientnew/clientnew.wasm`](clientnew/clientnew.wasm) - the binary itself (9.4 MB), the source of everything below.

### Raw data ([`clientnew/data/`](clientnew/data/))

- [`data/strings.txt`](clientnew/data/strings.txt) - every printable string (~108k).
- [`data/sections.txt`](clientnew/data/sections.txt) - the wasm section table.
- [`data/imports.txt`](clientnew/data/imports.txt) - 361 imports (minified wasm-bindgen interop).
- [`data/exports.txt`](clientnew/data/exports.txt) - 46 exports.
- [`data/changelog.txt`](clientnew/data/changelog.txt) - the in-game changelog, embedded verbatim.

### Decoded content ([`clientnew/content/`](clientnew/content/))

The embedded i18n table, decoded per namespace. For each namespace there is a
`<ns>.json` (all keys and values, all languages), and for the main namespaces a
`<ns>.en.json` (best-effort English name/fullname/description per id) plus a
`<ns>.ids.txt` (plain id list).

- Petals: [`Petals.json`](clientnew/content/Petals.json), [`Petals.en.json`](clientnew/content/Petals.en.json), [`petals.ids.txt`](clientnew/content/petals.ids.txt)
- Mobs: [`Mobs.json`](clientnew/content/Mobs.json), [`Mobs.en.json`](clientnew/content/Mobs.en.json), [`mobs.ids.txt`](clientnew/content/mobs.ids.txt)
- Talents: [`Talents.json`](clientnew/content/Talents.json), [`Talents.en.json`](clientnew/content/Talents.en.json), [`talents.ids.txt`](clientnew/content/talents.ids.txt)
- Rarities: [`Rarities.json`](clientnew/content/Rarities.json), [`Rarities.en.json`](clientnew/content/Rarities.en.json), [`rarities.ids.txt`](clientnew/content/rarities.ids.txt)
- Maps: [`Maps.json`](clientnew/content/Maps.json), [`Maps.en.json`](clientnew/content/Maps.en.json), [`maps.ids.txt`](clientnew/content/maps.ids.txt)
- Achievements: [`Achievements.json`](clientnew/content/Achievements.json), [`achievements.ids.txt`](clientnew/content/achievements.ids.txt)
- Other namespaces: [`UI.json`](clientnew/content/UI.json), [`Chat.json`](clientnew/content/Chat.json), [`Flower.json`](clientnew/content/Flower.json), [`Tutorial.json`](clientnew/content/Tutorial.json), [`Colors.json`](clientnew/content/Colors.json), [`Drops.json`](clientnew/content/Drops.json), [`Attribute.json`](clientnew/content/Attribute.json), [`Article.json`](clientnew/content/Article.json), [`Sacrifice.json`](clientnew/content/Sacrifice.json)

### Assets ([`clientnew/svg/`](clientnew/svg/) and [`clientnew/maps/`](clientnew/maps/))

- [`svg/`](clientnew/svg/) - 148 vector assets (`asset_NNN.svg`): the petal, mob, and UI art, drawn in-engine with Skia.
- [`maps/`](clientnew/maps/) - 92 map grids decoded from the embedded gzip blobs. Each is a raw `grid_NNN.bin` byte grid plus a `grid_NNN.png` grayscale render for square grids. See [`maps/index.txt`](clientnew/maps/index.txt) for dimensions.

### Scripts ([`clientnew/scripts/`](clientnew/scripts/))

The extraction is fully reproducible from the binary:

- [`scripts/extract_content.js`](clientnew/scripts/extract_content.js) - `strings.txt` to `content/`.
- [`scripts/extract_assets.js`](clientnew/scripts/extract_assets.js) - `strings.txt` to `svg/` and `maps/` (needs `npm i pngjs`).

## Findings at a glance

- Tech stack: Rust plus wasm-bindgen; Skia (2D/vector) rendered on WebGL via glow; ICU for internationalization; a fixed 256 MB heap.
- Backend and third parties: game servers at `api.n.m28.io`; auth via `oauth2.florr.io` with Google, Apple, and Discord; Xsolla for payments. Only public OAuth endpoints and redirects are present; no secrets.
- Content inventory: 122 petals, 87 mobs, 113 talents, 10 rarities, 18 maps/biomes, 142 achievements.
- Rarities: common, unusual, rare, epic, legendary, mythic, ultra, super, unique, eternal.
- Assets: 148 SVG vector assets and 92 map grids ranging from 12x12 up to 246x246.

See [`clientnew/analysis.md`](clientnew/analysis.md) for the detailed breakdown, and
the section there on [what cannot be recovered from the binary alone](clientnew/analysis.md#what-cant-be-recovered-from-the-binary-alone)
(function names, the wire protocol, and the JS-to-wasm import mapping).

## Reproducing

Everything under [`clientnew/`](clientnew/) is re-derivable from
[`clientnew.wasm`](clientnew/clientnew.wasm):

```
cd clientnew
strings -n 5 clientnew.wasm > data/strings.txt
wasm-objdump -h clientnew.wasm > data/sections.txt          # wabt
wasm-objdump -x -j Import clientnew.wasm > data/imports.txt
wasm-objdump -x -j Export clientnew.wasm > data/exports.txt
node scripts/extract_content.js
npm i pngjs && node scripts/extract_assets.js
```

## License

See [LICENSE](LICENSE).
